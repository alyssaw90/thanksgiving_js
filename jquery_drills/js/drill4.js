$(function(){
	//Hide the first div, DONE
	// $("#first").first().hide();

	//Hide the first list in the first div, DONE
	// $("#first ul").first().hide();

	//Hide the first list in the second div, DONE
	// $("#second li").first().hide();

	//Hide the second div, DONE
	// $("#second").first().hide();

	//Hide the third item of the third list, DONE
	// $(".third li").last().hide();

	// Hide the first item of the first list in the first div, DONE
	// $("#first li").first().hide();

	// Set a red background on the first div, DONE
	// $("#first").css("background", "red");

	// Set a red background on the second list in the first div, DONE
	// $("#first ul").last().css("background", "red");

	// Set a red background on the second item of every list
	// $(".second li").css("background", "red");

	// Fade out the second div after 5 seconds, DONE
	// $("#second").fadeOut(3000);

	// Remove the first item of every list from the DOM (no hiding)

	// Add a fourth item to the second list in the first div
	// $("#first .second:last-child").append(<li>Second fourth item</li);

	// Add a fourth item to all three lists (with one function call)

	// Remove the second list from the first div and add it back to the second div

	// Remove any element that is clicked on

	// Remove any element in the first div that is clicked on
	// $("#first").on('click', function(){

	// });

	// Fade out any element that is clicked, after 3 seconds
	// $("#first").on('click', function(){
	// 	$(this).fadeOut(3000);
	// });

    // console.log('The dom is ready! (drill 4)');




});

